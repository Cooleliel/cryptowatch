import 'package:flutter/material.dart';

abstract final class AppColors {
  // Fond et surfaces (thème moderne mockup)
  static const background = Color(0xFFF4F6FB);
  static const card = Color(0xFFFFFFFF);
  static const cardBorder = Color(0xFFEAEFF8);
  static const surfaceElevated = Color(0xFFFFFFFF);
  
  // Couleurs d'accentuation
  static const primary = Color(0xFF2F66F6);
  static const primaryLight = Color(0xFFEEF3FF);
  static const primaryDark = Color(0xFF1E4CD8);

  // Badges & Pilules
  static const rankBadge = Color(0xFFEFF2F8);
  static const pillInactive = Color(0xFFEFF2F7);
  static const pillInactiveText = Color(0xFF6B7A90);

  // Typographie
  static const textPrimary = Color(0xFF131B2E);
  static const textSecondary = Color(0xFF7A8B9E);
  static const textTertiary = Color(0xFFA0AFC0);

  // Trading & Variations
  static const gain = Color(0xFF00C087);
  static const gainPill = Color(0xFFE6F9F3);
  static const loss = Color(0xFFFF4D6A);
  static const lossPill = Color(0xFFFFECEF);

  // Cryptos & Icônes
  static const bitcoin = Color(0xFFF7931A);
  static const ethereum = Color(0xFF627EEA);
  static const ripple = Color(0xFF23292F);
  static const tether = Color(0xFF26A17B);
  static const bnb = Color(0xFFF0B90B);
  static const solana = Color(0xFF9945FF);
  static const star = Color(0xFFFFB800);
}

